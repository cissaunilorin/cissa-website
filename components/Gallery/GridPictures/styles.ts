import { ImageProps, BoxProps } from '@chakra-ui/react';

export const gridPicProps: BoxProps = {
    backgroundRepeat: 'no-repeat',
    backgroundSize: 'cover',
    backgroundPosition: '0 0, 100% 0',
    width: '100%',
    height: 400,
}